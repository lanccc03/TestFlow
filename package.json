{
  "name": "testflow",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "dev:web": "pnpm --filter @testflow/web dev",
    "dev:backend": "cd backend && uv run python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:desktop": "pnpm --filter @testflow/desktop dev",
    "check:web": "pnpm --filter @testflow/web check",
    "check:desktop": "pnpm --filter @testflow/desktop check",
    "test:desktop": "pnpm --filter @testflow/desktop test",
    "check:backend": "cd backend && uv run ruff check .",
    "test:backend": "cd backend && uv run pytest",
    "check": "pnpm check:web && pnpm check:desktop && pnpm test:desktop && pnpm check:backend && pnpm test:backend"
  }
}
