## 项目范围

### 背景问题

当前已有一个基于 `pytest` 的自动化测试框架，用于座舱稳定性测试。测试执行能力已经具备，但测试用例编写依赖代码能力，导致以下问题：

- 非研发人员难以独立编写和维护用例。
- 用例逻辑、执行参数、执行历史和报告分散，缺少统一入口。
- 常用 SSH 命令和文件传输操作需要手工执行，复用和沉淀成本较高。

### 项目目标

开发一个前后端自动化测试工具，把现有测试框架封装成可视化、可管理、可复用的测试平台。用户可以通过关键字和参数编辑测试脚本，执行脚本，并查看执行历史与报告。

核心目标：

- 降低座舱稳定性测试用例编写门槛。
- 复用现有 `pytest` 自动化框架和 `stability/feature` 关键字函数。
- 提供脚本管理、执行管理、报告查看的一体化入口。
- 提供独立的 SSH/SCP 工具能力，支持命令库联想和常用操作沉淀。

### 目标用户

- 测试工程师：创建、维护、执行测试脚本，查看测试报告。
- 研发工程师：维护底层关键字函数、执行器和框架适配逻辑。
- 项目/质量负责人：查看执行历史、失败原因和稳定性趋势。

### 范围边界

#### 测试框架相关能力

1. 脚本编辑
   - 支持通过关键字步骤编排测试脚本。
   - 每个步骤包含关键字、参数、描述、启用/禁用状态。
   - 支持步骤排序、复制、删除和参数校验。

2. 脚本保存与管理
   - 支持创建、编辑、删除、复制脚本。
   - 支持脚本分组、标签、搜索和基础版本信息。
   - 支持区分草稿、已发布、已废弃等状态。

3. 脚本执行
   - 支持手动执行单个脚本。
   - 支持配置执行环境、目标设备、变量参数。
   - 后端将可视化脚本保存为 YAML，并交给平台新增的执行器解析执行。
   - 执行器负责调用现有 `pytest` 测试框架及 `stability/feature` 中的关键字函数。

4. 执行历史
   - 记录每次执行的脚本、版本、执行人、环境、开始时间、结束时间、状态和日志路径。
   - 支持按脚本、状态、时间、执行人筛选。

5. 执行报告
   - 展示执行结果、步骤明细、失败原因、日志和附件。
   - 支持查看历史报告。
   - 后续可扩展趋势统计和稳定性分析。

#### 独立工具能力

1. SSH 模拟终端
   - 支持在 Web 页面中连接目标设备或服务器。
   - 支持基础交互式命令输入、输出展示和连接状态提示。

2. SCP 文件传输
   - 支持本地与远端之间的文件上传、下载。
   - 支持传输进度、失败提示和基础重试。

3. 可编辑命令库
   - 支持维护常用 SSH 命令模板。
   - 命令可包含变量占位符，执行前填写。
   - SSH 终端输入时支持命令库联想。

### 测试框架适配说明

现阶段自动化框架代码尚未完整纳入本项目，因此先约定框架适配模型：

- `stability/feature` 目录下存放可执行函数。
- 每个函数对应一个可暴露给前端的关键字。
- 现有关键字函数暂无统一参数类型和返回值规范，平台需要新增关键字元数据描述。
- 关键字元数据需要包含名称、描述、参数定义、参数类型、默认值、是否必填和示例。
- 后端维护关键字注册表，把前端脚本步骤解析到具体函数。
- 平台侧需要新增执行器，负责按步骤顺序调用函数，并收集每一步的输入、输出、状态、耗时和错误信息。
- 执行环境和目标设备信息优先读取测试框架已有配置文件，平台不重复维护一套设备配置。
- 测试报告可以复用测试框架已有报告能力；如现有报告无法满足平台展示需求，平台可独立生成结构化报告。

示例执行链路：

1. 前端编辑关键字脚本。
2. 后端将脚本保存为 YAML 文件。
3. 用户发起执行并选择环境/设备。
4. 后端生成执行任务。
5. 执行器读取测试框架配置，解析 YAML 中的关键字步骤。
6. 执行器调用 `stability/feature` 中的函数，并驱动 `pytest` 执行流程。
7. 执行结果写入历史记录和报告。

### MVP 范围

首个版本建议聚焦以下能力：

- 关键字列表展示和参数说明。
- 可视化脚本编辑、保存、复制、删除。
- 单脚本手动执行。
- 执行状态跟踪和执行历史。
- 基础报告查看，包括步骤结果、日志和失败原因。
- SSH 终端基础连接与命令执行。
- 命令库维护和终端命令联想。

暂不放入首个版本：

- 定时任务和批量任务调度。
- 多人协同编辑。
- 复杂权限审批流。
- 图形化趋势大盘。
- 自动生成测试用例。
- 跨项目插件市场。

### 非功能要求

- 可维护性：关键字注册、脚本解析、任务执行、报告生成需要保持清晰边界。
- 可扩展性：后续应能新增关键字类型、执行环境和报告字段。
- 可追溯性：执行报告必须能追溯到脚本版本、执行参数和目标设备。
- 安全性：SSH/SCP 账号密码由用户手动输入，默认不持久化；敏感日志需要脱敏。
- 稳定性：执行任务失败不能影响平台主服务；任务状态需要可恢复或可查询。
- 易用性：前端应优先降低测试人员编写脚本的理解成本，错误提示要直接指向具体步骤和参数。

### 建议模块划分

- 前端 Web：脚本编辑器、脚本管理、执行历史、报告页面、SSH 终端、命令库。
- 后端 API：脚本管理、关键字管理、任务管理、报告查询、SSH/SCP 接口。
- 执行器：YAML 脚本解析、测试框架配置读取、关键字调用、日志采集、状态回传。
- 数据存储：脚本元数据、脚本 YAML 文件、脚本版本、关键字元数据、执行任务、执行报告、命令库。
- 框架适配层：连接平台脚本模型和现有 `pytest` / `stability/feature` 实现。

### 关键数据对象

- Keyword：关键字名称、描述、参数定义、所属模块、示例、启用状态。
- TestScript：脚本名称、描述、分组、标签、状态、当前版本。
- TestScriptVersion：脚本版本号、YAML 文件路径或内容摘要、变量定义、创建人、创建时间。
- ExecutionTask：执行任务、脚本版本、执行环境、目标设备、状态、触发人。
- ExecutionReport：任务结果、步骤结果、日志、附件、失败原因。
- CommandTemplate：命令名称、命令内容、变量定义、分类、使用说明。

### 已确认设计决策

- 平台侧需要新增执行器，而不是完全依赖现有 `pytest` 入口。
- 现有关键字函数暂无统一参数类型和返回值规范，需要平台侧补充关键字元数据规范。
- 测试脚本保存为 YAML 文件，平台维护脚本元数据和版本信息。
- 执行环境和目标设备信息来自测试框架已有配置文件。
- SSH/SCP 凭据由用户手动输入账号密码，默认不持久化。
- 报告优先接入测试框架已有报告部分；如果展示、检索或结构化能力不足，平台可独立实现报告。

### 后续需要细化

- YAML 脚本结构规范，包括脚本基本信息、变量、步骤、关键字、参数和启用状态。
- 关键字元数据维护方式：通过配置文件、代码装饰器、扫描生成，还是后台手动维护。
- 执行器与 `pytest` 的集成方式：直接调用函数、生成临时测试用例，还是通过插件接入。
- 测试框架配置文件的读取路径、格式和多环境选择方式。
- 报告数据结构，以及平台报告与测试框架报告之间的映射关系。
